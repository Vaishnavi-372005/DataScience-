
Practical No 1

Nice—going with **Ubuntu** is the *best choice* for CloudSim too 😄🐧
Let’s set it up cleanly so you can **run simulations smoothly + explain in viva**.

---

# ☁️ Installing CloudSim on Ubuntu

---

## 🧰 1. Install Required Tools

Open Terminal and run:

```bash
sudo apt update
sudo apt install default-jdk eclipse git
```

### ✅ Check Java:

```bash
java -version
```

👉 If version shows → you're good 👍

---

## 📦 2. Download CloudSim

```bash
git clone https://github.com/Cloudslab/cloudsim.git
cd cloudsim
```

---

## 🧑‍💻 3. Open in Eclipse IDE

* Open Eclipse
* Click: **File → Open Projects from File System**
* Select the `cloudsim` folder

👉 Eclipse will automatically detect the project

---

## ▶️ 4. Run Sample Simulation

Navigate inside project:

```
examples → org.cloudbus.cloudsim.examples
```

Run:

👉 `CloudSimExample1.java`

**How to run:**

* Right-click file → **Run As → Java Application**

---

## 💻 5. Sample Output

You’ll see something like:

```
Starting CloudSim simulation...
CloudSim completed!
```

---

# 🧠 What Happens in Simulation?

CloudSim simulates:

* 🏢 Datacenter → Physical servers
* 💻 VM → Virtual machine
* 📄 Cloudlet → Task/job

---

# 🎯 Viva Ready Explanation

You can say:

👉
**CloudSim is a Java-based simulation toolkit used to model cloud infrastructure like datacenters, virtual machines, and task scheduling policies without using real cloud hardware.**

---

# ⚠️ Common Errors + Fix

## ❌ “Java not found”

👉 Fix:

```bash
sudo apt install default-jdk
```

---

## ❌ Eclipse not opening

👉 Run:

```bash
eclipse
```

---

## ❌ Project not detected

👉 Use:

* “Import → Existing Projects”

---

# ⚡ Alternative (Without Eclipse)

If you want simple method:

```bash
cd cloudsim
javac -cp jars/*:. examples/org/cloudbus/cloudsim/examples/CloudSimExample1.java
java -cp jars/*:. org.cloudbus.cloudsim.examples.CloudSimExample1
```

---

# 💡 Small Tip (Exam + Practical)

👉 If viva teacher asks:
**“Why CloudSim?”**

Answer:

* Cost-effective 💰
* No real infrastructure needed
* Easy testing of scheduling algorithms

---

# 🙌 If you want next level

I can help you:

* 🔹 Create full simulation (Datacenter + VM + Cloudlet)
* 🔹 Draw diagram for exam
* 🔹 Give viva questions (very important 🔥)
* 🔹 Connect CloudSim with ML concepts (good for your branch!)

Just tell me what you need ✨

____________________________________________________________________________________________


Practical no 1 (Windows)

Nice—**CloudSim** is actually *much easier* than GreenCloud, especially on Windows 😄✨
Let’s go step-by-step so you can **install + run simulation + explain in viva** confidently.

---

# ☁️ What is CloudSim?

* A **Java-based simulation toolkit** for cloud computing
* Developed by Cloud Computing and Distributed Systems (CLOUDS) Lab
* Used to simulate:

  * Data centers 🏢
  * Virtual machines (VMs) 💻
  * Task scheduling (Cloudlets) 📊

---

# 💻 1. Requirements (Windows ✅)

You just need:

* ☕ Java JDK
* 🧑‍💻 Eclipse IDE (or IntelliJ)
* 📦 CloudSim library

---

# ⚙️ 2. Installation Steps

## 🔹 Step 1: Install Java

Check:

```bash id="rgcmzl"
java -version
```

👉 If not installed → download JDK (8 or 11 preferred)

---

## 🔹 Step 2: Install Eclipse

Download & install:
👉 Eclipse IDE for Java Developers

---

## 🔹 Step 3: Download CloudSim

* Download CloudSim zip (from GitHub or official site)
* Extract folder

---

## 🔹 Step 4: Import into Eclipse

1. Open Eclipse
2. Click:
   **File → New → Java Project**
3. Name: `CloudSimProject`

---

## 🔹 Step 5: Add CloudSim Libraries

* Right-click project → **Build Path → Configure Build Path**
* Go to **Libraries**
* Click **Add External JARs**
* Select all `.jar` files from CloudSim folder

---

# ▶️ 3. Simple Simulation Code

Create a new Java class:

```java id="hglaf9"
import org.cloudbus.cloudsim.*;
import java.util.*;

public class CloudSimExample {
    public static void main(String[] args) {

        try {
            int num_user = 1;
            Calendar calendar = Calendar.getInstance();
            boolean trace_flag = false;

            CloudSim.init(num_user, calendar, trace_flag);

            System.out.println("CloudSim simulation started...");

            // No heavy setup (basic example)
            CloudSim.startSimulation();
            CloudSim.stopSimulation();

            System.out.println("Simulation finished!");
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
```

---

# ▶️ 4. Run Simulation

👉 In Eclipse:

* Right-click file → **Run As → Java Application**

✅ Output:

```
CloudSim simulation started...
Simulation finished!
```

---

# 📊 5. What Happens in Simulation?

CloudSim simulates:

* 🏢 **Datacenter** → Physical servers
* 💻 **VM (Virtual Machine)** → Runs tasks
* 📄 **Cloudlet** → Task/job

---

# 🎯 Viva Explanation (SUPER IMPORTANT)

You can say this confidently:

👉
**CloudSim is a Java-based toolkit used to model and simulate cloud computing environments such as data centers, virtual machines, and resource provisioning policies without requiring real cloud infrastructure.**

---

# 🧠 Key Terms (Exam Ready)

| Term       | Meaning                        |
| ---------- | ------------------------------ |
| Datacenter | Collection of physical servers |
| Host       | Physical machine               |
| VM         | Virtual machine                |
| Cloudlet   | Task/job                       |
| Broker     | Assigns tasks to VMs           |

---

# ⚡ GreenCloud vs CloudSim (Important)

| Feature    | CloudSim ☁️         | GreenCloud ⚡      |
| ---------- | ------------------- | ----------------- |
| Language   | Java                | C++ + TCL         |
| OS         | Windows ✅           | Linux             |
| Focus      | Resource management | Energy efficiency |
| Difficulty | Easy                | Hard              |

---

# 💡 Pro Tip (For You)

Since you're doing AI & DS:

👉 You can extend CloudSim project into:

* Task scheduling optimization (ML 🤖)
* Load balancing algorithms
* Energy-aware scheduling

---

# 🙌 Want next?

I can help you with:

* ✔️ Full CloudSim project (with VM + Cloudlet)
* ✔️ Viva questions & answers
* ✔️ Diagram for exam
* ✔️ Advanced code (broker, scheduling)

Just tell me 👍✨
____________________________________________________________________________________________________________________________________________________________________



Practical No 2

Got it—this is a classic lab/viva topic, and honestly GreenCloud setup can feel confusing at first 😄. I’ll break it down **very simply + step-by-step** so you can *actually run the simulation without errors*.

---

# 🌩️ What is GreenCloud?

* A simulator for **cloud data centers**
* Built on top of NS2
* Used to study:

  * Energy consumption ⚡
  * Network performance 📡
  * Cloud task scheduling ☁️

---

# 💻 1. System Requirements

👉 Best environment:

* **Linux (Ubuntu recommended)** 🐧
* OR use **Virtual Machine (VMware / VirtualBox)**

⚠️ GreenCloud does NOT work properly on Windows CMD directly.

---

# ⚙️ 2. Installation Steps (Ubuntu)

## Step 1: Install Dependencies

Open Terminal and run:

```bash
sudo apt update
sudo apt install build-essential autoconf automake libxmu-dev
```

---

## Step 2: Install NS2

```bash
sudo apt install ns2
```

Check installation:

```bash
ns
```

👉 If `%` appears → Installed successfully ✅

---

## Step 3: Download GreenCloud

```bash
git clone https://github.com/GreenCloud/greencloud.git
cd greencloud
```

---

## Step 4: Install GreenCloud

```bash
./install.sh
```

👉 This compiles the simulator.

---

# ▶️ 3. Running Simulation

Now your main doubt:
**"Where to run .tcl file?"**

👉 Answer: **In Terminal (NOT CMD)**

---

## Step 1: Go to folder

```bash
cd greencloud
```

---

## Step 2: Run your script

```bash
ns greencloud.tcl
```

---

# ❌ Common Error You Got:

> **"The system cannot find the path specified"**

👉 Happens because:

* You are using **Windows CMD**
* Or wrong folder path

✔️ Fix:

* Use **Ubuntu terminal**
* Make sure `.tcl` file is inside the folder

---

# 📄 Example Simple Script (Your Code Fixed)

```tcl
set ns [new Simulator]

set n1 [$ns node]
set n2 [$ns node]

$ns duplex-link $n1 $n2 10Mb 10ms DropTail

set tcp [new Agent/TCP]
$ns attach-agent $n1 $tcp

set sink [new Agent/TCPSink]
$ns attach-agent $n2 $sink

$ns connect $tcp $sink

set ftp [new Application/FTP]
$ftp attach-agent $tcp

$ns at 1.0 "$ftp start"
$ns at 5.0 "finish"

proc finish {} {
    global ns
    $ns halt
}

$ns run
```

---

# 📊 4. Output

After running:

* Simulation runs in terminal
* You may get:

  * Trace file 📄
  * Energy stats ⚡
  * Network logs

---

# 🎯 Viva Points (Important!)

You can confidently say:

* GreenCloud is used for **energy-aware cloud simulation**
* Built on **NS2**
* Written in **C++ + TCL**
* Helps analyze:

  * Power consumption
  * Cooling systems
  * Data center efficiency

---

# 💡 Quick Tip (Exam/Practical)

If they ask:

👉 *"Why not Windows?"*
Answer:

> GreenCloud depends on Linux libraries and NS2 compatibility, so it runs best on Ubuntu.

---

# 🙌 If you want next:

I can help you with:

* ✔️ Viva questions + answers
* ✔️ Diagram for explanation
* ✔️ Output analysis (how to explain in exam)
* ✔️ Fix your exact error step-by-step

Just tell me 👍

_______________________________________________________________________________________________________

Practical No . 3

Love this question—this is exactly the kind of thing that makes your practical/viva stand out 😄🔥
Let’s build a **custom scheduling algorithm in CloudSim (Ubuntu/Windows both work)** step-by-step.

---

# ☁️ Goal

👉 Simulate a cloud scenario using CloudSim
👉 Implement a **NEW scheduling algorithm (not default)**

We’ll implement:

# 🚀 **Priority-Based Scheduling Algorithm**

---

# 🧠 Idea (Simple)

* Each **Cloudlet (task)** has a **priority**
* Higher priority → executed first
* CloudSim default = FCFS (First Come First Serve)
  👉 We will override this behavior

---

# 🏗️ 1. Cloud Architecture

We will simulate:

* 🏢 Datacenter
* 💻 VM (Virtual Machine)
* 📄 Cloudlets (tasks with priority)
* 🤖 Custom Broker (our logic)

---

# 💻 2. Custom Scheduling Code

👉 Create a new Java file:

## 🔹 Step 1: Create Custom Broker

```java
import org.cloudbus.cloudsim.*;
import java.util.*;

public class PriorityBroker extends DatacenterBroker {

    public PriorityBroker(String name) throws Exception {
        super(name);
    }

    @Override
    protected void submitCloudlets() {
        // Sort cloudlets based on priority (higher first)
        Collections.sort(getCloudletList(), new Comparator<Cloudlet>() {
            @Override
            public int compare(Cloudlet c1, Cloudlet c2) {
                return Integer.compare((int)c2.getCloudletLength(), (int)c1.getCloudletLength());
            }
        });

        super.submitCloudlets();
    }
}
```

---

## 🔹 Step 2: Main Simulation Code

```java
import org.cloudbus.cloudsim.*;
import java.util.*;

public class PrioritySchedulingSimulation {

    public static void main(String[] args) {

        try {
            int num_user = 1;
            Calendar calendar = Calendar.getInstance();
            boolean trace_flag = false;

            CloudSim.init(num_user, calendar, trace_flag);

            Datacenter datacenter0 = createDatacenter("Datacenter_0");

            PriorityBroker broker = new PriorityBroker("Broker");

            int brokerId = broker.getId();

            List<Vm> vmlist = new ArrayList<>();

            Vm vm1 = new Vm(0, brokerId, 1000, 1, 512, 1000, 10000, "Xen", new CloudletSchedulerTimeShared());
            vmlist.add(vm1);

            broker.submitVmList(vmlist);

            List<Cloudlet> cloudletList = new ArrayList<>();

            for(int i=0; i<5; i++) {
                Cloudlet cloudlet = new Cloudlet(
                        i, 1000 + i*500, 1, 300, 300,
                        new UtilizationModelFull(),
                        new UtilizationModelFull(),
                        new UtilizationModelFull()
                );
                cloudlet.setUserId(brokerId);
                cloudletList.add(cloudlet);
            }

            broker.submitCloudletList(cloudletList);

            CloudSim.startSimulation();

            List<Cloudlet> resultList = broker.getCloudletReceivedList();

            CloudSim.stopSimulation();

            printCloudletList(resultList);

        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    private static Datacenter createDatacenter(String name) {

        List<Host> hostList = new ArrayList<>();

        List<Pe> peList = new ArrayList<>();
        peList.add(new Pe(0, new PeProvisionerSimple(1000)));

        hostList.add(new Host(
                0,
                new RamProvisionerSimple(2048),
                new BwProvisionerSimple(10000),
                1000000,
                peList,
                new VmSchedulerTimeShared(peList)
        ));

        DatacenterCharacteristics characteristics = new DatacenterCharacteristics(
                "x86", "Linux", "Xen",
                hostList, 10.0, 3.0, 0.05, 0.001, 0.0
        );

        Datacenter datacenter = null;
        try {
            datacenter = new Datacenter(name, characteristics, new VmAllocationPolicySimple(hostList), new LinkedList<>(), 0);
        } catch (Exception e) {
            e.printStackTrace();
        }

        return datacenter;
    }

    private static void printCloudletList(List<Cloudlet> list) {
        for (Cloudlet cloudlet : list) {
            System.out.println("Cloudlet " + cloudlet.getCloudletId() +
                    " finished at time " + cloudlet.getFinishTime());
        }
    }
}
```

---

# ▶️ 3. Run Simulation

👉 In Eclipse:

* Right-click → **Run As → Java Application**

---

# 📊 4. Output Meaning

You’ll see:

```bash
Cloudlet 4 finished at time ...
Cloudlet 3 finished at time ...
```

👉 Higher length = treated as higher priority
👉 Executed first ✅

---

# 🎯 Viva Explanation (VERY IMPORTANT 🔥)

Say this:

👉
**“We implemented a custom priority-based scheduling algorithm by extending the DatacenterBroker class and overriding the cloudlet submission method. Cloudlets were sorted based on priority before execution, unlike default FCFS scheduling in CloudSim.”**

---

# ⚡ Other Algorithms You Can Try (Impress Examiner 😎)

* Shortest Job First (SJF)
* Round Robin
* Min-Min / Max-Min
* Genetic Algorithm (advanced 🤖)

---

# 💡 Pro Tip (High Marks)

👉 Add comparison:

* Default vs Custom
* Measure:

  * Execution time
  * Throughput

---

# 🙌 Want next?

I can help you:

* ✔️ Add graphs for result analysis
* ✔️ Create viva questions (very important 🔥)
* ✔️ Convert this into mini project
* ✔️ Add ML-based scheduling (next level 🚀)

Just tell me 👍✨
________________________________________________________________________________________________________________________________________________________

Practical No .  5

Great practical question—this is something you’ll actually use a lot while working with VMs 😄✨
Let’s go step-by-step with **simple + viva-ready methods** to transfer files between virtual machines.

---

# 💻 📂 File Transfer Between Virtual Machines

👉 Situation:

* VM1 → Sender
* VM2 → Receiver

---

# 🥇 Method 1: Using SCP (Best for Ubuntu/Linux VMs) 🚀

👉 Uses secure copy over network

---

## 🔧 Step 1: Find IP of both VMs

Run in both VMs:

```bash
ip a
```

👉 Example:

* VM1 → `192.168.1.10`
* VM2 → `192.168.1.11`

---

## 🔧 Step 2: Transfer File

From **VM1 → VM2**:

```bash
scp file.txt username@192.168.1.11:/home/username/
```

👉 Example:

```bash
scp data.csv user@192.168.1.11:/home/user/
```

---

## 🔐 Password

👉 Enter VM2 password → transfer starts ✅

---

# 🥈 Method 2: Using Shared Folder (Easy for Viva 👍)

Works in Oracle VM VirtualBox

---

## 🔧 Steps:

1. Open VM Settings
2. Go to **Shared Folders**
3. Add a folder from host system
4. Enable:

   * Auto-mount
   * Make permanent

---

## 📂 Access inside VM:

```bash
cd /media/sf_sharedfolder
```

👉 Both VMs can access same folder → copy files easily

---

# 🥉 Method 3: Using USB / Drag & Drop

👉 Simple but not always reliable

* Enable:

  * Drag & Drop → Bidirectional
  * Clipboard → Bidirectional

---

# 🥇 Method 4: Using SSH + SFTP

👉 Advanced but useful

From VM1:

```bash
sftp username@192.168.1.11
```

Then:

```bash
put file.txt
```

---

# 🌐 Method 5: Using Python HTTP Server (Creative 😎)

👉 Run on VM1:

```bash
python3 -m http.server 8000
```

👉 Open in VM2 browser:

```
http://192.168.1.10:8000
```

👉 Download file easily

---

# ⚠️ Common Problems + Fix

## ❌ “Connection refused”

👉 Fix:

```bash
sudo apt install openssh-server
sudo service ssh start
```

---

## ❌ Same IP issue

👉 Set network mode:

* Bridged Adapter OR
* Internal Network

---

# 🎯 Viva Answer (Short & Perfect)

👉
**“Files can be transferred between virtual machines using network-based methods like SCP or SFTP, shared folders in virtualization software, or by setting up a local server. SCP is commonly used because it provides secure and fast file transfer over SSH.”**

---

# 💡 Best Recommendation

👉 For your practical:

* Use **SCP (professional)**
* OR **Shared Folder (easy demo)**

---

# 🙌 Want next?

I can help you:

* ✔️ Show diagram for exam
* ✔️ Give viva questions
* ✔️ Troubleshoot your VM network step-by-step

Just tell me what you’re using (VirtualBox / VMware) 👍✨

______________________________________________________________________________________________________________________________

Practical No . 5 (windows)

Got it—you want **file transfer between virtual machines on Windows** 👍✨
I’ll give you the **most practical methods** (easy + viva-ready).

---

# 💻 📂 File Transfer Between VMs (Windows Setup)

## 🥇 Method 1: Using SCP (Best & Professional 🚀)

👉 Works if your VMs are **Ubuntu/Linux inside Windows (VirtualBox/VMware)**

---

## 🔧 Step 1: Get IP Address of VM2

Inside VM2 (Ubuntu):

```bash
ip a
```

👉 Example: `192.168.56.102`

---

## 🔧 Step 2: Transfer File from VM1 → VM2

From VM1 terminal:

```bash
scp file.txt username@192.168.56.102:/home/username/
```

---

## 🔐 If SSH not installed:

```bash
sudo apt install openssh-server
sudo service ssh start
```

---

## ✅ Why this is best?

* Secure 🔐
* Fast ⚡
* Used in real-world cloud systems ☁️

---

# 🥈 Method 2: Shared Folder (Easiest 😄)

Using Oracle VM VirtualBox

---

## 🔧 Steps:

1. Open VM Settings
2. Go to **Shared Folders**
3. Add a folder from Windows
4. Enable:

   * ✔ Auto-mount
   * ✔ Make Permanent

---

## 📂 Access inside VM:

```bash
cd /media/sf_foldername
```

👉 Copy files normally between both VMs

---

## ✅ Best for:

* Quick demo in practical
* No network setup needed

---

# 🥉 Method 3: Drag & Drop / Copy-Paste

👉 Enable in VirtualBox:

* Drag & Drop → Bidirectional
* Clipboard → Bidirectional

⚠️ Sometimes buggy, not reliable

---

# 🌐 Method 4: Python Server (Cool Trick 😎)

## On VM1:

```bash
python3 -m http.server 8000
```

## On VM2 (browser):

```
http://<VM1-IP>:8000
```

👉 Download files easily

---

# ⚠️ Important (Most Students Miss This!)

## 🔌 Network Setting MUST be:

In VirtualBox:

* Adapter → **Bridged** OR
* Adapter → **Host-Only**

❌ NAT only → VMs cannot communicate properly

---

# ❌ Common Errors

### ❌ “Connection timed out”

👉 Fix:

* Check IP
* Check same network
* Start SSH

---

### ❌ “Permission denied”

👉 Fix:

* Correct username
* Correct password

---

# 🎯 Viva Answer (Perfect 💯)

👉
**“File transfer between virtual machines in Windows can be done using SCP over SSH, shared folders provided by virtualization software, or by setting up a local server. SCP is the most secure and commonly used method.”**

---

# 💡 My Suggestion for YOU

Since you're doing practical:

👉 Use:

* **Shared Folder → for easy demo**
* **SCP → for viva explanation (impress examiner 😎)**

---

# 🙌 Need help?

Tell me:

* Are your VMs Ubuntu or Windows?
* VirtualBox or VMware?

I’ll guide you exactly step-by-step for your setup 🤝✨
___________________________________________________________________________________________________________________

Practocal No . 6

Perfect—this is a **classic CloudSim practical question** and super scoring if you explain it clearly 😄✨
Let’s build a **simple simulation: 1 Datacenter → 1 Host → 1 VM → 1 Cloudlet**

---

# ☁️ Goal (What we’ll do)

👉 Using CloudSim:

* 🏢 1 Datacenter
* 🖥️ 1 Host
* 💻 1 VM
* 📄 1 Cloudlet (task)

---

# 🧠 Architecture (Easy to visualize)

```
Datacenter
   ↓
 Host (Physical Machine)
   ↓
 VM (Virtual Machine)
   ↓
 Cloudlet (Task)
```

---

# 💻 Full Working Code (VERY IMPORTANT 🔥)

Create a Java file:

```java
import org.cloudbus.cloudsim.*;
import java.util.*;

public class SingleCloudletSimulation {

    public static void main(String[] args) {

        try {
            // Step 1: Initialize CloudSim
            int num_user = 1;
            Calendar calendar = Calendar.getInstance();
            boolean trace_flag = false;

            CloudSim.init(num_user, calendar, trace_flag);

            // Step 2: Create Datacenter
            Datacenter datacenter = createDatacenter("Datacenter_1");

            // Step 3: Create Broker
            DatacenterBroker broker = new DatacenterBroker("Broker");
            int brokerId = broker.getId();

            // Step 4: Create VM
            Vm vm = new Vm(
                    0, brokerId,
                    1000, // MIPS
                    1,    // CPU cores
                    512,  // RAM
                    1000, // bandwidth
                    10000,// storage
                    "Xen",
                    new CloudletSchedulerTimeShared()
            );

            List<Vm> vmList = new ArrayList<>();
            vmList.add(vm);
            broker.submitVmList(vmList);

            // Step 5: Create Cloudlet
            Cloudlet cloudlet = new Cloudlet(
                    0,       // ID
                    4000,    // Length
                    1,       // CPU cores
                    300,     // file size
                    300,     // output size
                    new UtilizationModelFull(),
                    new UtilizationModelFull(),
                    new UtilizationModelFull()
            );

            cloudlet.setUserId(brokerId);

            List<Cloudlet> cloudletList = new ArrayList<>();
            cloudletList.add(cloudlet);
            broker.submitCloudletList(cloudletList);

            // Step 6: Run Simulation
            CloudSim.startSimulation();

            List<Cloudlet> resultList = broker.getCloudletReceivedList();

            CloudSim.stopSimulation();

            // Step 7: Print Output
            for (Cloudlet c : resultList) {
                System.out.println("Cloudlet ID: " + c.getCloudletId());
                System.out.println("Status: " + c.getStatus());
                System.out.println("VM ID: " + c.getVmId());
                System.out.println("Execution Time: " + c.getActualCPUTime());
                System.out.println("Finish Time: " + c.getFinishTime());
            }

        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    // Datacenter creation
    private static Datacenter createDatacenter(String name) {

        List<Host> hostList = new ArrayList<>();

        // Create Processing Element (CPU)
        List<Pe> peList = new ArrayList<>();
        peList.add(new Pe(0, new PeProvisionerSimple(1000)));

        // Create Host
        Host host = new Host(
                0,
                new RamProvisionerSimple(2048),
                new BwProvisionerSimple(10000),
                1000000,
                peList,
                new VmSchedulerTimeShared(peList)
        );

        hostList.add(host);

        // Datacenter characteristics
        DatacenterCharacteristics characteristics =
                new DatacenterCharacteristics(
                        "x86", "Linux", "Xen",
                        hostList,
                        10.0, 3.0, 0.05,
                        0.001, 0.0
                );

        Datacenter datacenter = null;

        try {
            datacenter = new Datacenter(
                    name,
                    characteristics,
                    new VmAllocationPolicySimple(hostList),
                    new LinkedList<>(),
                    0
            );
        } catch (Exception e) {
            e.printStackTrace();
        }

        return datacenter;
    }
}
```

---

# ▶️ How to Run (Ubuntu / Windows Eclipse)

* Right-click file
  👉 **Run As → Java Application**

---

# 📊 Expected Output

```
Cloudlet ID: 0
Status: SUCCESS
VM ID: 0
Execution Time: ...
Finish Time: ...
```

---

# 🧠 Concept Explanation (VERY EASY)

* **Datacenter** → Collection of hosts
* **Host** → Physical machine
* **VM** → Virtual machine running on host
* **Cloudlet** → Task/job

👉 Flow:
Cloudlet → VM → Host → Datacenter

---

# 🎯 Viva Answer (Perfect 💯)

👉
**“We simulated a cloud environment using CloudSim by creating one datacenter with a single host. A virtual machine was deployed on the host, and a cloudlet was assigned to the VM for execution. The simulation shows how tasks are processed in a cloud infrastructure.”**

---

# 💡 Small Tip to Impress Examiner 😎

If asked:
👉 *“What happens if more cloudlets?”*

Say:

* Scheduling becomes important
* Load balancing needed
* Can use custom algorithms

---

_____________________________________________________________________________________________________________________

Practical no . 7

Awesome—this is a super practical skill and also a favorite viva question ☁️😄
Let’s walk through **creating a VM on Google Cloud step-by-step** (simple + exam-ready).

---

# ☁️ What is Google Cloud Platform?

* A cloud service by Google
* Provides:

  * Virtual machines 💻
  * Storage 📦
  * AI/ML services 🤖

👉 VM service is called **Compute Engine**

---

# 💻 Creating a Virtual Machine (VM)

---

## 🥇 Step 1: Open Console

👉 Go to: [https://console.cloud.google.com](https://console.cloud.google.com)
👉 Login with your Google account

---

## 🥈 Step 2: Create / Select Project

* Click **Project dropdown (top bar)**
* Click **New Project**
* Give name → Create

---

## 🥉 Step 3: Go to Compute Engine

* Menu (☰) → **Compute Engine → VM Instances**
* Click **Enable API** (first time only)

---

## ⚙️ Step 4: Create VM Instance

Click **Create Instance**

---

### 🔧 Important Settings (Very Important for Viva 🔥)

* **Name** → `my-vm`
* **Region** → (choose nearest, e.g., Mumbai)
* **Machine type** → `e2-micro` (free tier)

---

### 💿 Boot Disk

* Click **Change**
* Select:

  * OS → **Ubuntu**
  * Version → Latest LTS

---

### 🔐 Firewall

✔ Allow:

* HTTP
* HTTPS

---

## ▶️ Step 5: Create

👉 Click **Create**
⏳ Wait 20–30 seconds

---

# 🔗 Step 6: Connect to VM

Click **SSH button** in browser

👉 Terminal will open like:

```bash id="b0s0qg"
Welcome to Ubuntu
```

---

# 🧪 Step 7: Test VM

Run:

```bash id="lzawhh"
sudo apt update
```

👉 Your VM is working ✅

---

# 📊 What You Just Created

* 🌍 Remote computer in cloud
* 💻 Runs Linux (Ubuntu)
* 🔗 Accessible via SSH

---

# 🎯 Viva Answer (Perfect 💯)

👉
**“A virtual machine in Google Cloud Platform is created using Compute Engine by selecting machine type, region, and operating system. The VM can then be accessed remotely using SSH for executing applications.”**

---

# ⚠️ Common Mistakes

❌ Not enabling Compute Engine
❌ Wrong region
❌ Forgetting firewall rules
❌ Billing not enabled

---

# 💡 Pro Tips

* Use **e2-micro** → Free tier 💰
* Stop VM when not in use → Save cost
* Use SSH instead of downloading software

---

# 🚀 Bonus (Impress Examiner 😎)

Say this:

👉

* VM = Infrastructure as a Service (IaaS)
* Fully scalable
* Pay-as-you-go model

---

_______________________________________________________________________________________________________